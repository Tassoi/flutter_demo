# Flutter Template Agent Guide

## 项目定位

本仓库用于建设一个可复用、可裁剪、可持续升级的 Flutter 项目脚手架，目标是减少新项目在工程初始化阶段的重复工作，而不是预先实现具体业务。

所有 Agent 在实现需求时必须遵守以下原则：

1. 先完成稳定、可运行、可测试的工程底座，再增加可选能力。
2. 默认生成的项目必须能够直接执行、理解和删除示例模块，不依赖未启用的可选功能。
3. 不设计万能基类、万能 Repository、万能 ViewModel 或与具体业务强耦合的抽象。
4. 同一类能力只保留一套默认实现。第一阶段不要同时引入多套状态管理、路由或网络方案。
5. 新依赖必须解决明确问题，并评估维护状态、许可证、平台支持、包体影响和替换成本。
6. 未经用户明确要求，不得扩大阶段范围或提前实现第二阶段功能。

## 交付阶段与需求范围

### 第一阶段：基础必选

第一阶段只实现以下 20 项。它们构成所有生成项目默认具备的工程基线。

1. **项目初始化**：支持配置项目名称、应用显示名称、包名、组织名、描述和目标平台；生成后可以直接执行 `flutter run`。
2. **统一目录结构**：使用 `app/`、`core/`、`features/`、`shared/` 划分应用组装、基础设施、业务功能和跨功能复用代码。
3. **多环境配置**：默认支持 `dev`、`staging`、`prod`，可分别配置 API 地址、日志级别、应用名称和包名后缀。
4. **配置与敏感信息管理**：区分可提交配置与敏感配置，提供示例配置文件，禁止把真实密钥写入源码或版本库。
5. **应用启动流程**：集中处理 Flutter Binding、环境读取、依赖初始化、异常监听和 `runApp`，并明确初始化顺序及失败策略。
6. **路由管理**：支持声明式路由、参数解析、嵌套路由、未知页面和重定向；认证相关重定向接口可以预留，但认证实现属于第二阶段。
7. **状态管理**：选择一套默认方案并规定状态边界、生命周期、异步状态和测试方式；第一阶段不并行维护多套实现。
8. **网络请求层**：支持 Base URL、超时、公共请求头、响应解析、请求取消、日志拦截及可替换的凭据注入接口。
9. **统一错误模型**：将网络、服务端、解析、配置和业务错误转换为稳定的应用错误类型，不向 UI 泄漏底层异常细节。
10. **本地存储**：提供普通键值存储和安全存储接口，隔离具体插件，并支持测试替身。
11. **日志系统**：区分日志级别和环境，支持结构化上下文；生产日志不得包含 Token、密码或完整个人数据。
12. **主题系统**：集中管理颜色、字体、字号、间距、圆角和亮暗主题，避免业务页面散落硬编码设计值。
13. **资源管理**：统一管理图片、普通 SVG、字体和其他静态资源，并提供类型安全的访问方式。
14. **基础组件**：提供加载、空状态、错误状态、确认弹窗和消息反馈等高频组件；组件必须保持轻量、可组合、无业务语义。
15. **代码规范**：固定支持的 Dart/Flutter 版本范围，配置格式化、Lint、静态分析和命名约定。
16. **测试基础设施**：提供单元测试、Widget 测试、测试替身和至少一个可运行示例，形成新增功能与修复缺陷的测试标准。
17. **示例业务模块**：提供一个可完整删除的 Feature，用于演示路由、状态、网络、错误处理和测试之间的协作方式。
18. **自动化检查**：CI 至少执行格式检查、静态分析和测试，并校验需要提交的生成产物是否最新。
19. **工程文档**：记录环境准备、初始化、运行、目录边界、配置、测试、构建和常见问题。
20. **依赖升级策略**：明确 SDK 与核心依赖的版本策略、升级步骤、兼容性验证和回滚方式。

### 第二阶段：仅保留的增强能力

只有第一阶段达到完成定义，或者用户明确要求提前处理时，才可以实现以下功能：

21. **国际化**：支持语言切换、复数、日期与数字格式化、缺失文案检查和类型安全的文案访问。
22. **SVG 转字体**：以原始 SVG 为唯一设计源，生成字体文件和 Dart `IconData` 映射；必须保证 codepoint 稳定、输出可复现，并在 CI 中检查生成产物。
23. **认证模块**：提供登录、退出、凭据安全存储、凭据刷新、会话失效和路由重定向；具体第三方登录方式按项目需求接入。
34. **品牌资源生成**：从源资源生成应用图标、启动资源和环境标识，并覆盖所选平台要求的尺寸与配置。

### 明确不在当前范围内

除上述需求外，不实现原列表中的其他可选能力，包括通用代码生成框架、独立依赖注入容器、数据库方案、权限中心、推送通知、分析与崩溃上报、实时通信、离线缓存、应用更新、多平台扩展包、Mock 服务、自动发布和脚手架 CLI。

如果第一阶段某项功能自身需要有限的生成步骤，例如类型安全的资源访问代码，可以在该功能边界内实现，但不得借此扩展成通用代码生成平台。

## 目标项目结构

在没有更具体且经过确认的架构决策前，使用以下结构：

```text
lib/
├── main.dart                     # 最小入口，只选择环境并启动应用
├── app/                          # 应用组装、启动、路由、主题和全局配置
├── core/                         # 网络、存储、错误、日志等稳定基础能力
├── features/                     # 按业务功能纵向组织的模块
└── shared/                       # 真正跨 Feature 复用的无业务组件与工具
assets/                           # 图片、普通 SVG、字体等静态资源
test/                             # 镜像 lib/ 边界组织的测试
tool/                             # 仓库级生成与检查工具，仅按需创建
```

依赖方向必须清晰：

1. `app/` 可以组装 `core/`、`features/` 和 `shared/`。
2. `features/` 可以依赖公开的 `core/`、`shared/` 接口，但 Feature 之间不得随意访问内部实现。
3. `core/` 不得依赖具体 Feature，也不得包含具体业务规则。
4. `shared/` 只存放已经确认被多个 Feature 复用的内容，不作为临时代码堆放目录。
5. 示例 Feature 必须能在不修改基础设施实现的情况下完整删除。

## 技术决策规则

1. 引入状态管理、路由、网络、序列化或存储依赖前，先记录选择理由、替代方案和主要权衡。
2. 优先使用 Flutter/Dart 官方能力和仓库已有模式；第三方包只承担清晰、有限的职责。
3. 不允许为了“以后可能需要”提前增加抽象层、配置项、平台代码或依赖。
4. 对外暴露稳定接口，把插件类型限制在基础设施实现内部，避免业务层直接依赖插件 API。
5. 环境差异必须来自明确配置，不得通过散落的 `kDebugMode` 或平台判断隐式改变业务行为。
6. 所有默认值都必须安全、可解释，并在文档或详细注释中说明覆盖方式。
7. 破坏性变更必须附迁移说明；能够保持向后兼容时，不无理由改变公开接口。

## 常用命令

当 Flutter 工程文件已经创建后，使用以下命令。若仓库仍处于文档或初始化阶段，不得假装这些命令已经成功运行。

```bash
flutter pub get
flutter run --dart-define=APP_ENV=dev
dart format --output=none --set-exit-if-changed .
flutter analyze
flutter test
```

如果项目采用代码生成，必须使用仓库文档中固定的生成命令，并在变更生成输入后重新生成。不得手工修改带有生成标记的文件。

## 实施工作流

1. 修改前先阅读相关源码、测试、配置和文档，确认当前模式以及工作区已有变更。
2. 将需求映射到第一阶段或第二阶段；范围不明确时选择不扩大功能面的最小实现，并记录假设。
3. 先定义行为和可验证的验收条件，再修改实现。
4. 保持改动聚焦，不顺手重构无关模块，不覆盖或回退用户已有改动。
5. 新增依赖时同步更新依赖说明、平台要求和测试策略。
6. 修改实现后同步更新相关测试、注释、示例和工程文档。
7. 按风险执行格式化、静态分析、测试和必要的平台构建，并如实报告未能运行的检查。
8. 除非用户明确要求，不得创建 Git commit、推送代码、发布产物或修改外部系统。

## 注释与文档规范

本项目要求 Agent 为复杂代码编写充分、准确、可长期维护的注释。注释是实现的一部分，不得在功能完成后作为可有可无的补充。

### 必须使用详细文档注释的内容

以下内容应使用 Dart 文档注释 `///`：

1. 对外公开的类、扩展、函数、属性和可复用 Widget。
2. 状态管理单元、Repository 接口、网络拦截器、存储接口、错误类型和配置模型。
3. 框架扩展点、生命周期入口以及需要调用方遵守约束的 API。
4. 行为无法仅从名称和类型准确推断的私有抽象。

详细文档注释应按实际复杂度说明：

1. 该类型或 API 的目的、职责边界以及明确不负责的内容。
2. 参数、返回值、状态变化、副作用和可能抛出或转换的错误。
3. 初始化与释放顺序、异步时序、并发限制、缓存规则和重试行为。
4. 安全与隐私约束、环境差异、平台限制和兼容性原因。
5. 非直观用法的最小示例，以及调用方必须维持的不变量。

### 必须使用行内注释的内容

以下逻辑必须在关键分支前写清楚原因：

1. 非显而易见的业务规则、边界条件和失败降级策略。
2. 复杂条件判断、状态机转换、缓存失效、去重、重试和请求取消。
3. 异步或并发执行顺序，以及顺序改变后可能产生的竞态问题。
4. 平台差异、第三方 API 异常行为、兼容性处理和临时 workaround。
5. 数据修正、时间/单位换算、序列化兼容和安全过滤。
6. 因性能、内存、包体或可测试性作出的非直观取舍。

注释优先解释 `why`、约束和取舍，同时补足调用者真正需要知道的 `how`。不得只是把代码逐句翻译成自然语言，也不得为简单赋值、直观 UI 层级、常规 getter/setter 或明显的空值判断制造噪声。

临时方案必须包含触发条件、移除条件以及可追踪的 TODO/Issue 信息。修改已有文件时沿用其语言和风格；注释与实现冲突时，必须在同一变更中修正注释。生成文件必须标记来源和重新生成方式，不在生成文件内堆积手写说明。

## 测试规范

测试是完成条件，不是可选收尾工作。

1. 测试文件使用 `*_test.dart` 命名，并尽量镜像 `lib/` 的目录结构。
2. 新功能至少覆盖正常路径、一个关键边界和一个主要失败路径；纯展示型改动可以用有针对性的 Widget 测试替代不适用的单元测试。
3. 修复缺陷必须先复现问题或建立失败证据，并增加能够阻止同类回归的测试。
4. 网络、时间、随机数、存储、平台通道和环境配置必须可替换，测试不得依赖真实服务、真实密钥或不稳定外部状态。
5. 网络层重点测试请求配置、错误映射、取消、敏感信息过滤和凭据注入边界。
6. 状态管理重点测试加载、成功、空数据、失败、重试和销毁后的行为。
7. 路由重点测试参数解析、未知页面和重定向；认证重定向的完整测试留到第二阶段。
8. 存储重点测试默认值、读写失败、清理和敏感数据不得进入普通存储。
9. 可复用 Widget 至少验证关键状态、交互和无障碍语义；涉及布局时覆盖有代表性的窄屏尺寸。
10. 测试必须确定、可独立运行，不依赖执行顺序，不使用无依据的延时等待。

每次代码变更至少执行与改动直接相关的测试。交付前原则上按以下顺序验证：

```bash
# 1. 格式检查
dart format --output=none --set-exit-if-changed .

# 2. 静态分析，必须零 error
flutter analyze

# 3. 全量测试
flutter test
```

如果变更涉及原生平台代码、字体、应用图标、启动资源或构建配置，还必须执行对应平台的实际构建；条件允许时在模拟器或真机验证。无法执行的检查必须在交付报告中说明原因和剩余风险。

## 完成定义

一项需求只有同时满足以下条件才可以标记完成：

1. 实现与当前阶段范围和验收条件一致，没有夹带范围外功能。
2. 公开接口、复杂逻辑、平台限制和关键取舍具有准确且足够详细的注释。
3. 正常、边界和主要失败行为得到与风险相称的测试覆盖。
4. 格式检查、静态分析和相关测试通过，或已明确记录无法执行的原因。
5. 示例、配置模板、生成产物和工程文档与实现同步。
6. 不包含真实密钥、凭据、个人数据、内部生产地址或敏感日志。
7. 已检查最终 diff，没有覆盖用户改动、无关重构或意外生成文件。
8. 已说明剩余风险、平台验证缺口和必要的回滚方式。

## 密钥与安全

1. 禁止提交 API Key、Token、证书、私钥、密码、真实用户数据或生产环境凭据。
2. 示例配置只能使用明显无效的占位值，并提供安全注入说明。
3. 日志、错误信息和测试快照必须脱敏。
4. 敏感数据只能进入安全存储；普通偏好存储不得保存认证凭据。
5. 涉及删除、覆盖、签名、发布、生产配置或外部系统写入时，必须先确认授权和精确作用范围。

## 提交规范

只有用户明确要求提交时，才创建 Git commit。提交使用 Conventional Commits：

```text
feat(network): add unified application error mapping
fix(storage): prevent credentials from using preferences
docs: define scaffold delivery phases
```

提交前必须检查 diff 和验证结果。不得把无关文件或用户未要求的生成产物混入提交。

## Goal Mode 工作流

只有用户明确输入 `/goal`、明确要求进入 Goal Mode，或客户端已经将当前任务置于 Goal Mode 时，才启用本流程。普通开发请求不得自行创建 Goal。

### Goal 初始化

1. 使用客户端提供的 Goal 工具注册用户的完整目标；仅在目标确实完成或满足工具规定的阻塞条件时更新最终状态。
2. 在当前项目根目录查找已有 `goal-[num]/`，使用下一个递增编号创建新目录，不得覆盖已有 Goal。
3. 创建以下三个文件：

```text
goal-[num]/
├── input.md
├── plan.md
└── tasks.md
```

4. `input.md` 必须逐字保存用户触发 Goal 的原始输入，不得改写、摘要或补充推测。
5. `plan.md` 必须记录需求分析、阶段范围、验收条件、上下文、明确假设、风险、执行方案、验证方式和回滚方案。
6. `tasks.md` 必须把计划拆分为小而可独立验证的任务。每个任务预留状态、实现记录、验证证据、剩余风险和下一步字段，并提前标出每三个任务后的全面检查节点。
7. Goal 初始化完成前不得修改业务源码、工程配置或依赖。
8. 第一轮只完成 Goal 注册和上述文件初始化，然后只输出 `GOAL_INIT_DONE` 并结束本轮，让客户端继续推进。

### 每轮执行

1. 每次新会话或上下文压缩后，完整读取当前 Goal 的 `input.md`、`plan.md` 和 `tasks.md`。
2. 使用可用的计划或 Todo 工具登记本轮工作，只选择 `tasks.md` 中第一个未完成任务。
3. 每轮只执行一个任务，不提前实现后续任务，也不擅自扩大目标范围。
4. 修改前确认任务验收条件；修改后检查源码、注释、测试、diff、日志、静态分析和必要构建证据。
5. 不得只口头声称“100% 有信心”。如果证据不足，继续诊断、补测试或修正实现，直到达到与风险相称的高置信度。
6. 完成任务后更新 `tasks.md`，记录实际变更、执行过的命令、结果、剩余风险和下一任务。
7. 除非用户在 Goal 中明确要求，Goal Mode 也不得自动创建 commit、推送、发布或修改外部系统。
8. 每轮结束时向用户简短汇报当前任务和验证结果，然后停止输出，由客户端决定是否继续下一轮。

### 全面检查节点

每完成三个实现任务，执行一次跨任务检查，并把证据写入 `tasks.md`：

1. 需求和阶段范围是否发生偏离。
2. 架构依赖、公开接口和删除可选模块的能力是否仍然成立。
3. 是否存在逻辑缺陷、竞态、资源泄漏或未处理错误。
4. 格式、类型检查、静态分析、测试和构建是否通过。
5. UI 是否在代表性尺寸下无溢出、遮挡和异常状态缺失。
6. 安全、隐私、日志脱敏和配置隔离是否正确。
7. 数据一致性、缓存、迁移和回滚策略是否充分。
8. 注释、示例、生成产物和文档是否同步。

### 阻塞与无人值守约束

Goal Mode 默认按无人值守方式推进。遇到不影响目标方向的不确定信息时，选择安全且最小的合理假设，写入 `plan.md` 或 `tasks.md`，继续不依赖该信息的部分。

如果继续操作需要新的用户授权、真实凭据、破坏性操作、生产环境变更或会实质改变目标方向，不得自行猜测或绕过限制；记录阻塞证据，并按客户端 Goal 工具的规则处理阻塞状态。

Goal Mode 中禁止：

1. 抢占用户设备焦点或启动未经要求的 GUI。
2. 重启本机、关闭网络或影响用户其他进程。
3. 删除重要数据或使用未经确认的破坏性命令。
4. 修改生产配置、真实密钥、认证、支付、签名或发布状态。
5. 在 Goal 初始化完成前修改项目实现。
6. 一轮执行多个任务或跳过第一个未完成任务。
7. 擅自扩大需求范围或添加本项目明确排除的功能。
8. 伪造测试、构建、文档核对或外部系统验证结果。

### Goal 完成

所有任务完成后必须执行最终全面 Review，从用户体验、需求范围、架构、代码、注释、安全、隐私、错误处理、数据一致性、测试、构建、文档和回滚角度检查整个目标。

修复所有已知高风险问题并记录最终证据后，才可以把 Goal 标记为完成。最终报告应简洁说明已交付内容、验证结果、剩余的非阻塞风险和必要的后续动作。
