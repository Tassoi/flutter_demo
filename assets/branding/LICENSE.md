# 品牌源资源权利声明

本目录当前包含一组由本仓库为生成流程验证创建的中性几何占位图。它们不包含客户名称、
商标、人物、照片或第三方 Logo，也不得被描述为任何下游项目的正式品牌。占位图可随本模板
使用和替换；正式项目仍必须在发布前换成项目方拥有或已取得明确授权的源资源。

当前占位源覆盖以下固定输入：

- `app_icon.png`：不透明的 1024 x 1024 完整应用图标源。
- `app_icon_foreground.png`：透明的 1024 x 1024 Android Adaptive Icon 前景源。
- `app_icon_background.png`：不透明的 1024 x 1024 Android Adaptive Icon 背景源。
- `app_icon_monochrome.png`：透明的 1024 x 1024 Android 13+ 单色图标源。
- `splash_logo.png`：透明的 1024 x 1024 启动 Logo 源。

替换正式资源时，维护者必须确认其拥有面向目标应用、地区、平台和分发渠道的使用与再分发
权利，并在本文件中记录来源、权利持有人、授权范围和必要的署名/限制。工具依赖的 MIT
许可证不覆盖输入图片权利，也不能替代维护者的法律确认。
